# excel-correlation
I kept seeing the question in data work:  "Does a higher critic score actually mean more sales?"  So I built a Python script to find out.  I wrote a Correlation Analysis tool that takes any CSV and automatically finds relationships between numeric columns.
